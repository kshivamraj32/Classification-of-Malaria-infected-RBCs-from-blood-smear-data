After pre-processing & thresholding steps, morphological operations were performed and marker-controlled
watershed algorithms were used . After feature extraction, SVM, ANN and random forest were used
for classification with 10 fold cross validation.
